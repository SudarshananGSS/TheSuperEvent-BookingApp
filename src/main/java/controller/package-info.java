/**
 * JavaFX controllers corresponding to application views.
 *
 * <p>Each controller handles user interactions from its FXML layout and
 * delegates business logic to the service layer.</p>
 */
package controller;